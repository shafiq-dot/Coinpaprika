<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CryptocurrencyAssignment" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Ensure the ActionBar is shown -->

    </style>

    <!-- Inherit the base theme -->
    <style name="Theme.CryptocurrencyAssignment" parent="Base.Theme.CryptocurrencyAssignment" />

</resources>